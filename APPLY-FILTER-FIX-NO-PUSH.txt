Omni Icon Vault 4.1.2 filter-fix (test before push)

Destination: repository root, normally ~/Documents/omni-icon-vault

1. Extract this overlay over the repo.
2. Run: python -m unittest discover -s tests -v
3. Run: omni-icons open
   This version detects an old in-memory API and restarts it automatically.
4. Run: python tools/verify_copy_filters.py
5. Hard refresh the browser once if an old tab is open.
6. Test Copy / filter -> SVG only and Glyph only.
7. Do NOT push until verified on the real icon index.
